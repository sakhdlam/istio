name: My Composite Action
description: A composite action that does something
outputs:
  result:
    description: 'The result of the composite action'
runs:
  using: "composite"
  steps:
    - name: Perform some task
      id: task
      run: |
        result=$(echo "Hello, World!")
        echo "result=$result" >> $GITHUB_OUTPUT


