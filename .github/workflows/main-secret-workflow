name: Main Secrets flow
on:
  workflow_dispatch:

jobs:
    setup-mesh:
      uses: sakhdlam/istio/.github/workflows/common-workflow.yaml@main
      secrets:
        ISTIO_SETUP_VAULT_ROLE_ID: "some-id-sakhie"
